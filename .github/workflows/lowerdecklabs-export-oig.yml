name: LowerDeckLabs - Export OIG Labels

on:
  workflow_dispatch:

jobs:
  export-oig:
    name: Export OIG API-Only Resources
    runs-on: ubuntu-latest

    environment:
      name: LowerDeckLabs

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Export OIG Labels
        run: |
          echo "This is a simplified workflow test"
          echo "Will add full export functionality once confirmed working"
          python3 scripts/okta_api_manager.py --help || echo "Script ready"
